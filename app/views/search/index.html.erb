
<h1 class=title> 郵便番号API </h1>


<div class="container">
        <%= form_with url: search_path, method: :get, local: true do |f| %>
          <%= f.text_field :postal_code, class: "search_form", placeholder: "郵便番号を入力してください" %>
        <% end %>
</div>


<%# form_with のurlは飛ばす先 %>
<%# 15 :postal_codeとはパラメーターの名前
<%# この後のコントローラーでparams[:postal_code]とすると、実際にフォームで入力した値が取得可能 %>